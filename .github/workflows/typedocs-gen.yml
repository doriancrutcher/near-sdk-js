name: Dispatch repo-b-update event

on:
  push:
    branches:
      - main

jobs:
  dispatch:
    runs-on: ubuntu-latest

    steps:
      - name: Dispatch event
        uses: peter-evans/repository-dispatch@v1
        with:
          token: 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
          event-type: repo-b-update
